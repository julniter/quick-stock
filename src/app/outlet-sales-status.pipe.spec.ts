import { OutletSalesStatusPipe } from './outlet-sales-status.pipe';

describe('OutletSalesStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new OutletSalesStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
