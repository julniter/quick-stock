import { JobOrderTypeValuePipe } from './job-order-type-value.pipe';

describe('JobOrderTypeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new JobOrderTypeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
