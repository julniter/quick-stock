import { JobOrderStatusValuePipe } from './job-order-status-value.pipe';

describe('JobOrderStatusValuePipe', () => {
  it('create an instance', () => {
    const pipe = new JobOrderStatusValuePipe();
    expect(pipe).toBeTruthy();
  });
});
