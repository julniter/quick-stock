import { InventorySnapshotStatusPipe } from './inventory-snapshot-status.pipe';

describe('InventorySnapshotStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new InventorySnapshotStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
